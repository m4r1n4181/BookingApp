<Window x:Class="BookingApp.View.TourAttendanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="TourAttendanceView" Height="450" Width="800">
    <Grid>
        <!-- Prikaz trenutne ture -->
        <DataGrid ItemsSource="{Binding KeyPoints}"
                  SelectedItem="{Binding Path=SelectedTour}"
                  IsReadOnly="True" SelectionMode="Single" 
                  AutoGenerateColumns="False" Height="325" HeadersVisibility="Column" FontSize="13" Width="661" Margin="50"
                  HorizontalAlignment="Left">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Keypoint" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="IsActive" Binding="{Binding IsActive}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
